const navToggle=document.querySelector('[data-nav-toggle]');const nav=document.querySelector('[data-nav]');if(navToggle&&nav){navToggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');navToggle.setAttribute('aria-expanded','false');}));}const RELEASE_API='https://api.github.com/repos/gh0sted5456-us/Dragonwilds-Sync/releases/latest';async function hydrateRelease(){const versionNodes=document.querySelectorAll('[data-sync-version]');const dateNodes=document.querySelectorAll('[data-sync-date]');const links=document.querySelectorAll('[data-sync-download]');if(!versionNodes.length&&!links.length)return;try{const response=await fetch(RELEASE_API,{headers:{Accept:'application/vnd.github+json'}});if(!response.ok)throw new Error('release lookup failed');const release=await response.json();const version=release.tag_name||release.name||'Latest release';const date=release.published_at?new Date(release.published_at).toLocaleDateString(undefined,{year:'numeric',month:'short',day:'numeric'}):'GitHub Releases';versionNodes.forEach(n=>n.textContent=version);dateNodes.forEach(n=>n.textContent=date);const assets=Array.isArray(release.assets)?release.assets:[];const windows=assets.find(a=>/\.exe$/i.test(a.name)&&!/headless/i.test(a.name));links.forEach(link=>{link.href=windows?.browser_download_url||release.html_url||'https://github.com/gh0sted5456-us/Dragonwilds-Sync/releases';});}catch{links.forEach(link=>link.href='https://github.com/gh0sted5456-us/Dragonwilds-Sync/releases');}}hydrateRelease();